/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "assets.aceternity.com",
      "skysoul159.github.io",
    ],
  },
};

export default nextConfig;
